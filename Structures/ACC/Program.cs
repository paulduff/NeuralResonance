using NeuralResonanceEngine.Protocol;

StructureHostApplication.Run(args, new StructureProfile(StructureId.Acc, "Izhikevich", "STDP", "Conflict monitoring relies on rapid error-related burst coding and recurrent control signals.", new DelayWindow(0, 0)));

