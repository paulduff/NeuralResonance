using NeuralResonanceEngine.Protocol;

StructureHostApplication.Run(args, new StructureProfile(StructureId.Parasubiculum, "LIF", "SynapticTaggingCapture", "Parasubiculum interfaces medial temporal and grid-like spatial computations; sparse persistent firing is represented with LIF plus consolidation capture.", new DelayWindow(3, 8)));

