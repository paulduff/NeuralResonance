using NeuralResonanceEngine.Protocol;

StructureHostApplication.Run(args, new StructureProfile(StructureId.InferiorOlive, "HH", "STDP", "Inferior olive subthreshold oscillation and synchronized complex spikes require conductance-based dynamics.", new DelayWindow(10, 15)));

