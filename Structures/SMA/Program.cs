using NeuralResonanceEngine.Protocol;

StructureHostApplication.Run(args, new StructureProfile(StructureId.Sma, "LIF", "STDP", "SMA sequence preparation can be represented with chained LIF assemblies for premotor timing.", new DelayWindow(8, 12)));

