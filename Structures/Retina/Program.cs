using NeuralResonanceEngine.Protocol;

StructureHostApplication.Run(args, new StructureProfile(StructureId.Retina, "HH", "BCM", "Retinal ganglion output combines graded photoreceptor adaptation with spike transfer through center-surround circuits, so a membrane-rich HH profile with BCM-like adaptation best matches retinal coding.", new DelayWindow(1, 4)));

